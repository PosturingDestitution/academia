/*
	need to make a getCh() function similar to getchar() but this function does not echo and is not canonical

	a few problems...this program is supposed to use conio.h (which i think is windows), and the this line while((ch = getCh()) != '\r') the carrage return? i dont know

*/
