/*

	need to make a getCh() function that does not echo and is not canonical

	termios.h: terminal I/O control (POISX)
	ncurses.h: terminal-independent screen handling
	unistd.h: POSIX operating system API
	fcntl.h: file control operations
*/
